package org.example.dipl.repo;

import org.example.dipl.model.StatusData;
import org.springframework.data.jpa.repository.JpaRepository;

public interface StatusDataRepository extends JpaRepository<StatusData, Long> {
}
